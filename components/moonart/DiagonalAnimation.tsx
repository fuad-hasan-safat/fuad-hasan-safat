import React from "react";
import styles from "./DiagonalAnimation.module.css"; // Import CSS module

const DiagonalAnimation = () => {
    return (
        <div className={styles.outer}>
        <div className={styles.box}></div>
        </div>
      );
};

export default DiagonalAnimation;
